{
  "name": "ai-driven-dev",
  "displayName": "AI-Driven Dev Config",
  "description": "Better VSCode code configuration to improve AI developer experience",
  "version": "0.1.0",
  "publisher": "ai-driven-dev",
  "repository": {
    "type": "git",
    "url": "https://github.com/ai-driven-dev/vscode.git"
  },
  "icon": "./icon.png",
  "engines": {
    "vscode": "^1.75.0"
  },
  "categories": [
    "Snippets",
    "Extension Packs",
    "Other"
  ],
  "extensionPack": [
    "ms-azuretools.vscode-docker"
  ],
  "contributes": {
    "snippets": [
      {
        "language": "typescript",
        "path": "./config/snippets/typescript.json"
      },
      {
        "language": "typescriptreact",
        "path": "./config/snippets/typescriptreact.json"
      }
    ],
    "configuration": {
      "properties": {
        "workbench.editor.wrapTabs": {
          "type": "boolean",
          "default": true,
          "description": "Wrap tabs for better visibility"
        },
        "workbench.editor.enablePreview": {
          "type": "boolean",
          "default": false,
          "description": "Disable preview mode for tabs"
        },
        "explorer.compactFolders": {
          "type": "boolean",
          "default": false,
          "description": "Disable compact folder display"
        },
        "css.hover.documentation": {
          "type": "boolean",
          "default": false,
          "description": "Disable CSS hover documentation"
        },
        "telemetry.enableTelemetry": {
          "type": "boolean",
          "default": false,
          "description": "Disable telemetry"
        },
        "telemetry.enableCrashReporter": {
          "type": "boolean",
          "default": false,
          "description": "Disable telemetry"
        },
        "editor.pasteAs.preferences": {
          "type": "array",
          "default": [
            "text.updateImports"
          ],
          "description": "Update imports on paste"
        },
        "editor.wordWrap": {
          "type": "string",
          "default": "on",
          "description": "Menu tabs wrapped for better visibility"
        },
        "editor.wordWrapColumn": {
          "type": "number",
          "default": 80,
          "description": "Menu tabs wrapped for better visibility"
        },
        "editor.wrappingIndent": {
          "type": "string",
          "default": "same",
          "description": "Menu tabs wrapped for better visibility"
        },
        "workbench.editor.enablePreviewFromQuickOpen": {
          "type": "boolean",
          "default": false,
          "description": "Disable preview mode for tabs"
        },
        "markdown.extension.toc.updateOnSave": {
          "type": "boolean",
          "default": true,
          "description": "Update TOC on save"
        },
        "typescript.updateImportsOnFileMove.enabled": {
          "type": "string",
          "default": "always",
          "description": "Update imports on file move"
        }
      }
    },
    "keybindings": [
      {
        "key": "ctrl+shift+v",
        "command": "editor.action.pasteAs",
        "args": {
          "preferences": [
            "text.updateImports"
          ]
        }
      },
      {
        "key": "ctrl+shift+l",
        "command": "editor.action.selectHighlights",
        "when": "editorTextFocus"
      }
    ]
  },
  "license": "GPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "build": "node setup.js",
    "prepare": "husky install",
    "publish-extension": "node setup.js && vsce package && vsce publish && rm *.vsix && git add package.json && git commit -m \"chore: bump version\"",
    "test": "vitest --watch=false"
  },
  "devDependencies": {
    "husky": "^8.0.0",
    "lint-staged": "^15.0.0",
    "vitest": "^2.1.8"
  },
  "lint-staged": {
    "README.md": [
      "npm run build"
    ],
    "settings.json": [
      "npm run build"
    ]
  }
}